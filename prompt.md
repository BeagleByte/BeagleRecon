I want automate parts of my recon process, port discovery and banner grabbing, I have in mind, 1. stage scan only for open ports, version of the the scan is -p- and -sX as extra option. Executed over subprocess, the result should be saved temporary as csv in a folder named output, filename should have date_hostip.csv. Then from here it should start banner grabbing, UPD: DNS, SNMP; TCP: DHCP, FTP,HTTP,HTTPS,IMAP,POP3,SMTP,RDP,RPC,SMB,SSH and Telnet. Oder ports should be scanned with the option -sCV. I have already for every service an own file, and I want for each service a own class, for non listest service also known as other service I want a OtherServicesClass, to handle events seperatly, which support maintaining the code in the future. The Final results of the grabbing should stored in a Markdown file. If markdown is possible with a CSS stylesheet, I want a folder with named assets, to store images and css files in this folder. In the end I want so support a nice report for an overview what the target has for open ports and services running. The last stage is a special class for each service and using nmap scripts for first vulnerabilty checks. And here also a report at the end of the scan on an extra overview.